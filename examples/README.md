# 📚 Примеры Workflow

Эта папка содержит готовые примеры workflow для быстрого старта.

## 📥 Как импортировать

### Способ 1: Через интерфейс
1. Откройте n8n: http://localhost:5678
2. Нажмите на меню (три линии) → "Import from File"
3. Выберите `.json` файл из этой папки
4. Workflow появится в вашем списке

### Способ 2: Через командную строку
```bash
# Из корня проекта
docker exec n8n-prototype n8n import:workflow --input=/backups/example-workflow-http.json
```

## 📋 Доступные примеры

### 1. `example-workflow-http.json` ⭐
**Описание:** Простой HTTP запрос к GitHub API

**Что делает:**
- Запрашивает информацию о пользователе n8n-io
- Обрабатывает JSON ответ
- Извлекает имя, количество подписчиков и репозиториев

**Триггер:** Manual (ручной запуск)

**Как использовать:**
1. Импортируйте workflow
2. Нажмите "Execute Workflow"
3. Посмотрите результат

**Модификации:**
- Замените username в URL на любой другой
- Добавьте обработку ошибок
- Сохраните результат в базу данных

---

### 2. `example-workflow-webhook.json` ⭐
**Описание:** Webhook обработчик для приема POST запросов

**Что делает:**
- Принимает POST запросы на `/webhook/test-webhook`
- Обрабатывает полученные данные
- Возвращает JSON ответ

**Триггер:** Webhook (автоматический при HTTP запросе)

**Как использовать:**
1. Импортируйте workflow
2. Активируйте workflow (переключатель "Active")
3. Отправьте тестовый запрос:
```bash
curl -X POST http://localhost:5678/webhook/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"name": "Test", "value": 123}'
```

**Модификации:**
- Добавьте валидацию входных данных
- Сохраните данные в базу
- Отправьте уведомление в Slack/Telegram

---

### 3. `example-workflow-schedule.json` 🆕
**Описание:** Автоматическая проверка репозитория по расписанию

**Что делает:**
- Выполняется каждые 5 минут автоматически
- Проверяет статистику репозитория n8n на GitHub
- Извлекает количество звезд, форков, открытых issues

**Триггер:** Schedule (каждые 5 минут)

**Как использовать:**
1. Импортируйте workflow
2. Настройте расписание в узле "Schedule Trigger" (если нужно)
3. Активируйте workflow (переключатель "Active")
4. Workflow будет выполняться автоматически!

**Модификации:**
- Измените расписание (каждый час, каждый день, etc)
- Добавьте отправку в базу данных
- Настройте алерты при превышении порога

---

### 4. `example-workflow-if-condition.json` 🆕
**Описание:** Условная логика с разветвлением

**Что делает:**
- Получает информацию о репозитории
- Проверяет количество звезд
- Направляет в разные ветки в зависимости от условия

**Триггер:** Manual (ручной запуск)

**Узлы:**
- IF узел для проверки условия (> 40000 звезд)
- Две ветки обработки (популярный / развивающийся)

**Как использовать:**
1. Импортируйте workflow
2. Нажмите "Execute Workflow"
3. Посмотрите как данные идут по разным путям

**Модификации:**
- Измените условие проверки
- Добавьте больше условий
- Добавьте действия для каждой ветки

---

## ⚠️ Важно: Совместимость с n8n 2.x

**Все примеры обновлены для n8n 2.x+**

Изменения по сравнению со старыми версиями:
- ✅ `n8n-nodes-base.start` → `n8n-nodes-base.manualTrigger`
- ✅ `n8n-nodes-base.function` → `n8n-nodes-base.code` (новая версия)
- ✅ Обновлены версии узлов

Если вы видите ошибку **"Unrecognized node type"**, убедитесь что:
1. Используете обновленные примеры из этой папки
2. n8n обновлен до последней версии: `make update`

---

## 🎨 Создайте свои примеры

После создания полезного workflow, экспортируйте его:

```bash
# Через Make
make backup

# Или вручную через интерфейс
# Workflow → ⚙️ → "Download"
```

Сохраните файл в эту папку и добавьте описание здесь!

## 💡 Идеи для следующих workflow

- **Email Alert**: Schedule + HTTP Check + IF + Email on Error
- **Data Pipeline**: Webhook → Transform → Database → Notification
- **API Aggregator**: Multiple HTTP Requests → Merge → Response
- **Monitoring**: Schedule → Health Check → Alert on failure
- **File Processor**: Webhook → Parse CSV → Split → Process → Store
- **Social Media**: Schedule → Get Posts → Filter → Repost
- **Form Handler**: Webhook → Validate → Save to DB → Send Email

## 🔗 Больше примеров

Ищите больше примеров на:
- [n8n.io/workflows](https://n8n.io/workflows) - официальная библиотека (1000+ workflows)
- [GitHub n8n-workflow-templates](https://github.com/n8n-io/n8n/tree/master/packages/cli/templates) - шаблоны
- [n8n Community Forum](https://community.n8n.io/c/workflows/) - пользовательские примеры

## 📊 Сравнение примеров

| Workflow | Сложность | Триггер | Узлов | Использование |
|----------|-----------|---------|-------|---------------|
| HTTP | 🟢 Легко | Manual | 3 | Начинающим |
| Webhook | 🟢 Легко | Webhook | 3 | API интеграции |
| Schedule | 🟡 Средне | Cron | 3 | Автоматизация |
| IF Condition | 🟡 Средне | Manual | 5 | Условная логика |

---

**Начните с простых примеров (HTTP, Webhook) и переходите к более сложным! 🚀**

