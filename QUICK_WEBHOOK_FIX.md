# ⚡ Быстрое решение проблемы с webhook (404)

## 🎯 В БРАУЗЕРЕ (http://localhost:5678):

```
1. Найдите workflow: "Пример: Webhook обработчик"
2. Откройте его (кликните)
3. В правом верхнем углу: [Inactive] → кликните → [Active] ✓
4. Должен стать ЗЕЛЕНЫМ
```

## 🧪 ПОСЛЕ АКТИВАЦИИ - ТЕСТ:

```bash
# Способ 1: Простой curl
curl -X POST http://localhost:5678/webhook/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"message": "Работает!"}'

# Способ 2: С красивым выводом
curl -X POST http://localhost:5678/webhook/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"test": true, "value": 123}' | jq '.'

# Способ 3: Готовый скрипт
./test-my-webhook.sh
```

## ✅ Ожидаемый успешный ответ:

```json
{
  "message": "Webhook получен успешно",
  "receivedData": { "message": "Работает!" },
  "timestamp": "2024-01-06T...",
  "source": "curl/..."
}
```

## 📋 Важно запомнить:

| URL | Когда использовать | Требует |
|-----|-------------------|---------|
| `/webhook-test/test-webhook` | Отладка | Нажать "Execute" каждый раз |
| `/webhook/test-webhook` | Постоянная работа | Активировать workflow один раз |

## 🔍 Диагностика:

```bash
# Проверить статус
./check-webhook.sh

# Посмотреть логи
make logs

# Проверить что n8n запущен
docker ps | grep n8n
```

---

**После активации webhook работает постоянно! 🚀**

