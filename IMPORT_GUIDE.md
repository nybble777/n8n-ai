# 📥 Пошаговая инструкция по импорту workflow

## Способ 1: Через веб-интерфейс (рекомендуется)

### Шаг 1: Откройте n8n
Перейдите на: **http://localhost:5678**

### Шаг 2: Импортируйте workflow
1. Нажмите на **меню** (три горизонтальные линии ☰) в левом верхнем углу
2. Выберите **"Import from File"** или **"Импортировать из файла"**
3. Нажмите **"Select file to import"**
4. Выберите файл: `/Users/nybble/projects/n8n/examples/example-workflow-webhook.json`
5. Нажмите **"Import"**

### Шаг 3: Активируйте workflow (ВАЖНО! ⚠️)
1. После импорта откроется workflow
2. В **правом верхнем углу** найдите переключатель **"Inactive"**
3. Нажмите на него, чтобы стал **"Active"** (зеленый цвет)
4. Workflow теперь работает!

### Шаг 4: Проверьте URL webhook
1. Кликните на узел **"Webhook"** (первый узел)
2. В правой панели вы увидите:
   - **Test URL**: `http://localhost:5678/webhook-test/test-webhook`
   - **Production URL**: `http://localhost:5678/webhook/test-webhook`

### Шаг 5: Протестируйте
Используйте **Test URL** для тестирования:

```bash
curl -X POST http://localhost:5678/webhook-test/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello!"}'
```

---

## Способ 2: Через командную строку (альтернатива)

```bash
# Скопируйте файл в контейнер
docker cp /Users/nybble/projects/n8n/examples/example-workflow-webhook.json n8n-prototype:/tmp/

# Импортируйте
docker exec n8n-prototype n8n import:workflow --input=/tmp/example-workflow-webhook.json

# Важно! Workflow все равно нужно активировать через веб-интерфейс
```

---

## ⚠️ Важные моменты

1. **Workflow должен быть активен** - иначе webhook не будет работать
2. **Test URL vs Production URL**:
   - Test URL: работает всегда (для разработки)
   - Production URL: работает только когда workflow активен

3. **URL формат**:
   - Test: `/webhook-test/ваш-путь`
   - Production: `/webhook/ваш-путь`

---

## ✅ Проверка что все работает

### 1. Проверьте что n8n запущен
```bash
docker ps | grep n8n-prototype
```

### 2. Проверьте список workflow
Откройте http://localhost:5678 - вы должны увидеть импортированный workflow

### 3. Убедитесь что workflow активен
Зеленая галочка "Active" должна быть включена

### 4. Протестируйте webhook
```bash
# Исправленный скрипт
cd /Users/nybble/projects/n8n
./test-my-webhook.sh

# Или напрямую curl
curl -X POST http://localhost:5678/webhook-test/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"test": true}'
```

---

## 🐛 Решение проблем

### Ошибка 404 - Not Found
- ✅ Workflow импортирован?
- ✅ Workflow активирован? (переключатель "Active")
- ✅ Правильный URL? (используйте Test URL)

### Ошибка 500 - Internal Server Error
- Проверьте логи: `make logs`
- Упростите Function узел для тестирования

### Connection Refused
- n8n не запущен: `make start`
- Проверьте порт: должен быть 5678

---

## 📸 Визуальная инструкция

После открытия http://localhost:5678:

```
┌─────────────────────────────────────────────────┐
│  n8n                                   [☰] Menu │
│                                                 │
│  Workflows                                      │
│  ┌─────────────────────────────────────────┐   │
│  │  + New Workflow                         │   │
│  └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘

1. Нажмите [☰] Menu
2. Выберите "Import from File"
3. Выберите файл webhook.json
4. После импорта включите "Active" ✓
```

---

## 🎯 Быстрый старт (копируй-вставь)

```bash
# 1. Убедитесь что n8n запущен
docker ps | grep n8n-prototype

# 2. Откройте n8n в браузере
open http://localhost:5678

# 3. Импортируйте через веб-интерфейс:
#    - Menu → Import from File
#    - Выберите: examples/example-workflow-webhook.json
#    - Активируйте workflow (переключатель "Active")

# 4. Протестируйте
cd /Users/nybble/projects/n8n
./test-my-webhook.sh
```

---

**После выполнения этих шагов webhook должен заработать!** 🚀

