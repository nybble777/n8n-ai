# 🚀 Быстрый старт n8n

## Шаг 1: Запуск (выберите один способ)

### Способ A: С помощью Make (рекомендуется)
```bash
make setup    # Первоначальная настройка
make start    # Запуск n8n
```

### Способ B: С помощью Docker Compose
```bash
docker-compose up -d
```

## Шаг 2: Откройте браузер

Перейдите на: **http://localhost:5678**

## Шаг 3: Создайте свой первый workflow

1. Нажмите "+ Add workflow"
2. Добавьте узел "Start"
3. Добавьте узел "HTTP Request"
4. Настройте запрос к любому API
5. Нажмите "Execute Workflow"

## 🎯 Полезные команды

```bash
make help      # Показать все команды
make logs      # Посмотреть логи
make backup    # Сохранить workflow
make stop      # Остановить n8n
```

## 📚 Примеры

В папке `examples/` находятся готовые примеры workflow:
- `example-workflow-http.json` - HTTP запросы
- `example-workflow-webhook.json` - Webhook обработчик

### Как импортировать пример:

1. Откройте n8n в браузере
2. Нажмите "⚙️" → "Import from File"
3. Выберите файл из папки `examples/`
4. Изучите и запустите workflow

## 🧪 Тестирование webhook

```bash
# После создания webhook в n8n:
curl -X POST http://localhost:5678/webhook/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"message": "Hello from curl!"}'
```

## ❓ Проблемы?

Смотрите полную документацию в `README.md`

---

**Готово! Начинайте прототипировать! 🎉**

