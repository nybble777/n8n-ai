# 🔄 Hot Reload в n8n

## ✅ Что работает автоматически (без перезапуска)

### 1. Изменения в Workflow
```
✅ Добавление/удаление узлов
✅ Настройка параметров узлов
✅ Изменение кода в Function узлах
✅ Изменение connections
✅ Переименование workflow
✅ Добавление заметок (Sticky Notes)
```
**Применяется:** Мгновенно при сохранении

### 2. Активация Workflow
```
✅ Включение/выключение workflow
✅ Изменение webhook путей
✅ Изменение cron расписаний
```
**Применяется:** Сразу после переключения

### 3. Credentials (учетные данные)
```
✅ Добавление credentials
✅ Изменение существующих
✅ Удаление credentials
```
**Применяется:** Сразу

### 4. Executions (выполнения)
```
✅ Просмотр истории выполнений
✅ Повторный запуск workflow
✅ Удаление старых executions
```
**Применяется:** В реальном времени

---

## ❌ Что требует перезапуска контейнера

### 1. Переменные окружения

**Изменения в `docker-compose.yml` или `.env`:**

```yaml
environment:
  - N8N_BASIC_AUTH_ACTIVE=true    # ← Требует restart
  - DB_TYPE=postgresdb            # ← Требует restart
  - N8N_PORT=8080                 # ← Требует restart
```

**Как применить:**
```bash
make restart
# или
docker-compose restart
```

### 2. Кастомные узлы (Custom Nodes)

**Изменения в `custom-nodes/`:**
- Добавление новых узлов
- Изменение кода существующих узлов
- Изменение package.json

**Как применить:**
```bash
make restart
```

### 3. Обновление n8n

```bash
make update
# Это делает pull + restart
```

### 4. Изменение портов или volumes

```yaml
ports:
  - "8080:5678"  # ← Требует down + up
volumes:
  - ./new-path:/home/node/.n8n  # ← Требует down + up
```

**Как применить:**
```bash
docker-compose down
docker-compose up -d
```

---

## 🚀 Режим разработки для Custom Nodes

Если вы разрабатываете кастомные узлы и хотите hot reload:

### Вариант 1: Использовать n8n локально (не Docker)

```bash
# Установка n8n глобально
npm install -g n8n

# Запуск с отслеживанием изменений
N8N_CUSTOM_EXTENSIONS=./custom-nodes n8n start --tunnel
```

**Преимущества:**
- ✅ Hot reload для custom nodes (с nodemon)
- ✅ Быстрый цикл разработки

**Недостатки:**
- ❌ Требует Node.js на хосте
- ❌ Нет изоляции Docker

### Вариант 2: Docker с volume mapping (текущая настройка)

```yaml
volumes:
  - ./custom-nodes:/home/node/.n8n/custom
```

**Применение изменений:**
```bash
# После изменения custom node
make restart
```

### Вариант 3: Docker Compose с nodemon (продвинутый)

Создайте `docker-compose.dev.yml`:

```yaml
version: '3.8'

services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n-dev
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - NODE_ENV=development
      - N8N_LOG_LEVEL=debug
      # ... остальные настройки
    volumes:
      - ./custom-nodes:/home/node/.n8n/custom
      - ./n8n-data:/home/node/.n8n
    command: >
      sh -c "npm install -g nodemon && 
             nodemon --watch /home/node/.n8n/custom 
             --exec 'n8n start'"
```

**Использование:**
```bash
docker-compose -f docker-compose.dev.yml up
```

---

## 💡 Практические советы

### Для разработки Workflow (обычное использование)

**Все работает из коробки!**
```
1. Открываете n8n: http://localhost:5678
2. Создаете/редактируете workflow
3. Изменения сохраняются автоматически
4. Execute workflow - тестируете
5. Всё работает без перезапуска! ✅
```

### Для тестирования Function узлов

```javascript
// Function узлы поддерживают полный hot reload
// Измените код → Execute Node → Сразу видите результат

console.log('Debug:', items[0].json);

return items.map(item => {
  return {
    json: {
      // Ваша логика
      result: item.json.value * 2
    }
  };
});
```

**Нет перезапуска! Работает сразу!** ✅

### Для разработки Custom Nodes

```bash
# 1. Создайте/измените узел в custom-nodes/
# 2. Перезапустите n8n
make restart

# 3. Узел появится в списке
# 4. Повторите при следующем изменении
```

---

## 🔍 Как проверить что изменения применились

### Проверка переменных окружения

```bash
# Посмотреть текущие env переменные
docker exec n8n-prototype env | grep N8N_
```

### Проверка custom nodes

```bash
# Список установленных custom nodes
docker exec n8n-prototype n8n list:workflow
```

### Проверка логов

```bash
# Смотреть логи в реальном времени
make logs
# или
docker logs n8n-prototype -f
```

---

## 🎯 Быстрая справка

| Изменение | Hot Reload? | Действие |
|-----------|-------------|----------|
| Workflow в UI | ✅ Да | Просто сохраните |
| Function код | ✅ Да | Execute Node |
| Credentials | ✅ Да | Сохранить |
| Переменные env | ❌ Нет | `make restart` |
| Custom nodes | ❌ Нет | `make restart` |
| Порты/Volumes | ❌ Нет | `docker-compose down && up` |
| Обновление n8n | ❌ Нет | `make update` |

---

## 🚀 Рекомендации для прототипирования

**Для быстрого прототипирования используйте:**

1. **Function узлы** вместо custom nodes
   - ✅ Hot reload из коробки
   - ✅ Быстрое тестирование
   - ✅ Логирование через console.log

2. **HTTP Request узлы** для интеграций
   - ✅ Изменения применяются сразу
   - ✅ Легко тестировать

3. **Webhook узлы** для входящих данных
   - ✅ Активация/деактивация мгновенная
   - ✅ Можно менять путь без перезапуска

4. **Переносите в custom nodes** только стабильный код
   - Когда логика отработана
   - Когда нужна переиспользуемость

---

## 💻 Примеры

### Пример: Быстрое тестирование логики (Function узел)

```javascript
// Изменяете код в UI
const data = items[0].json;

// Добавьте логирование для отладки
console.log('Input:', data);

const result = {
  processed: true,
  value: data.value * 2
};

console.log('Output:', result);

return [{ json: result }];

// Execute Node → видите результат сразу!
// Измените логику → Execute Node снова
// Никаких перезапусков! ✅
```

### Пример: Разработка Custom Node (требует restart)

```bash
# 1. Создайте узел
mkdir -p custom-nodes/my-custom-node
cd custom-nodes/my-custom-node

# 2. Создайте файлы узла
# MyCustomNode.node.ts, package.json, etc

# 3. Перезапустите n8n
cd /Users/nybble/projects/n8n
make restart

# 4. Узел доступен в UI
# 5. При изменении кода - снова restart
```

---

## 🎓 Итого

**Для 95% случаев (работа с workflow):**
- ✅ Hot reload работает отлично
- ✅ Все изменения в UI применяются мгновенно
- ✅ Перезапуск не нужен

**Для 5% случаев (системные изменения):**
- ❌ Требуется `make restart`
- Это нормально и быстро (~5-10 секунд)

**Для прототипирования:**
- 🎯 Используйте Function узлы
- 🎯 Всё работает без перезапусков
- 🎯 Быстрая итерация

---

**n8n отлично подходит для прототипирования с минимальными перезапусками! 🚀**

