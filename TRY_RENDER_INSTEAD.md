# 🎨 Деплой на Render.com (БЕЗ карты!)

## Почему Render.com?

- ✅ **НЕ требует кредитную карту**
- ✅ Полностью бесплатно
- ✅ PostgreSQL включен
- ✅ Проще чем Fly.io
- ✅ Автодеплой из GitHub

---

## 🚀 Быстрый старт (10 минут)

### Шаг 1: Push в GitHub

```bash
cd /Users/nybble/projects/n8n

# Инициализируйте git (если еще не сделано)
git init
git add .
git commit -m "Initial n8n setup for Render.com"

# Создайте репозиторий на GitHub
# https://github.com/new

# Добавьте remote (замените USERNAME и REPO)
git remote add origin https://github.com/USERNAME/REPO.git
git branch -M main
git push -u origin main
```

---

### Шаг 2: Зарегистрируйтесь на Render

1. Откройте: https://render.com
2. Sign Up (можно через GitHub - быстрее)
3. **Карта НЕ требуется!**

---

### Шаг 3: Создайте PostgreSQL базу

1. Dashboard → New → PostgreSQL
2. Name: `n8n-db`
3. Database: `n8n`
4. User: `n8n`
5. Region: **Frankfurt** (или ближайший)
6. Plan: **Free**
7. Create Database

**Скопируйте:**
- Internal Database URL (понадобится)

---

### Шаг 4: Создайте Web Service

1. Dashboard → New → Web Service
2. Connect Repository → выберите ваш репозиторий
3. Настройки:

```
Name: n8n-prototype
Region: Frankfurt
Branch: main
Runtime: Docker
Dockerfile Path: ./Dockerfile.render
Instance Type: Free
```

---

### Шаг 5: Добавьте Environment Variables

Нажмите "Advanced" и добавьте:

```bash
# Генерируйте пароль и ключ локально:
PASSWORD=$(openssl rand -base64 20)
ENCRYPTION_KEY=$(openssl rand -base64 32)
echo "Password: $PASSWORD"
echo "Encryption Key: $ENCRYPTION_KEY"
```

Затем добавьте в Render:

```
N8N_PORT=5678
N8N_PROTOCOL=https
N8N_HOST=n8n-prototype.onrender.com
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=[ваш сгенерированный пароль]
WEBHOOK_URL=https://n8n-prototype.onrender.com

DB_TYPE=postgresdb
DATABASE_URL=[Internal Database URL из шага 3]

N8N_ENCRYPTION_KEY=[ваш сгенерированный ключ]

GENERIC_TIMEZONE=Europe/Moscow
NODE_ENV=production
```

⚠️ Замените:
- `n8n-prototype` на имя вашего сервиса
- `[ваш пароль]` на сгенерированный пароль
- `[Internal Database URL]` на URL из шага 3
- `[ваш ключ]` на сгенерированный encryption key

---

### Шаг 6: Деплой!

1. Нажмите **"Create Web Service"**
2. Ждите 3-5 минут (первый деплой)
3. Готово!

---

### Шаг 7: Откройте n8n

URL будет показан в Dashboard:
```
https://n8n-prototype.onrender.com
```

**Логин:**
- Username: `admin`
- Password: (тот что вы сгенерировали)

---

## ✅ Готово!

n8n работает на Render.com!

---

## 🔄 Автоматические обновления

При каждом `git push` Render автоматически задеплоит новую версию!

```bash
# Внесите изменения
git add .
git commit -m "Update"
git push

# Render автоматически задеплоит
```

---

## ⚠️ Важно знать

**Засыпание:**
- Render бесплатный tier засыпает через 15 минут неактивности
- Первый запрос после сна займет ~30 секунд

**Решение:**
- Upgrade до платного плана ($7/месяц) - всегда активен
- Или используйте ping service (UptimeRobot бесплатно)

---

## 🔧 Настройка Ping (опционально)

Чтобы n8n не засыпал:

1. Зарегистрируйтесь на https://uptimerobot.com (бесплатно)
2. Add New Monitor
3. Monitor Type: HTTP(s)
4. URL: `https://n8n-prototype.onrender.com`
5. Monitoring Interval: 5 минут

UptimeRobot будет пинговать каждые 5 минут и n8n не заснет!

---

## 📊 Статус и логи

```
Dashboard → Ваш сервис:
- Logs: Логи в реальном времени
- Metrics: Использование ресурсов
- Environment: Переменные окружения
```

---

## 💰 Стоимость

**Бесплатно:**
- ✅ Unlimited web services
- ✅ PostgreSQL (90 дней, потом нужно использовать или удалится)
- ✅ 100GB bandwidth/месяц
- ⚠️ Засыпает через 15 мин

**Платно ($7/месяц):**
- ✅ Не засыпает
- ✅ Больше ресурсов

---

## 🔄 Миграция с Render на Fly.io (если захотите позже)

```bash
# Экспортируйте данные
# Через n8n UI: Settings → Export

# Задеплойте на Fly.io (когда добавите карту)
# Импортируйте данные через UI
```

---

## 📚 Дополнительно

- Render Docs: https://render.com/docs
- n8n Docs: https://docs.n8n.io/

---

**Render.com - отличная альтернатива без карты! 🎉**

