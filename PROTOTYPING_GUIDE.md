# 🎨 Руководство по прототипированию в n8n

## 🎯 Философия прототипирования

При прототипировании в n8n главное - **быстрая итерация** и **визуальная обратная связь**.

## 📋 Рекомендуемый процесс

### 1. Начните с минимальной версии
- Создайте простейший workflow с 2-3 узлами
- Проверьте, что основная идея работает
- Только потом добавляйте сложность

### 2. Тестируйте каждый узел отдельно
- Используйте кнопку "Execute Node" для тестирования
- Проверяйте данные на каждом этапе
- Не переходите дальше, пока узел не работает правильно

### 3. Используйте функции отладки
```javascript
// В узле Function используйте console.log
console.log('Received data:', items[0].json);

// Или возвращайте отладочную информацию
return [{
  json: {
    debug: true,
    originalData: items[0].json,
    processedData: result
  }
}];
```

## 🔧 Полезные паттерны

### Обработка ошибок
```javascript
// В узле Function
try {
  const result = processData(items[0].json);
  return [{ json: { success: true, data: result } }];
} catch (error) {
  return [{ json: { success: false, error: error.message } }];
}
```

### Условная логика (IF узел)
- Используйте IF узел для разветвления логики
- Создавайте разные пути для успеха и ошибок
- Тестируйте оба пути

### Работа с API

#### Простой GET запрос
```
Узел: HTTP Request
Method: GET
URL: https://api.example.com/data
Authentication: None (или настройте)
```

#### POST с данными
```
Узел: HTTP Request
Method: POST
URL: https://api.example.com/submit
Body: JSON
Body Parameters:
{
  "key": "={{ $json.value }}"
}
```

### Webhook для приема данных
```
1. Добавьте узел Webhook
2. Настройте Path: /my-webhook
3. Method: POST
4. Response Mode: "Using Respond to Webhook Node"

URL для тестирования:
http://localhost:5678/webhook/my-webhook
```

## 🎨 Примеры типичных workflow

### 1. Мониторинг API
```
Cron (каждые 5 минут)
  → HTTP Request (проверка статуса)
  → IF (status === 200)
    → true: Log "OK"
    → false: Send Alert
```

### 2. Обработка Webhook
```
Webhook (POST /data)
  → Function (валидация)
  → HTTP Request (сохранение в БД)
  → Respond to Webhook (ответ клиенту)
```

### 3. ETL процесс
```
Start
  → HTTP Request (получить данные)
  → Function (трансформация)
  → Split In Batches (обработка по частям)
  → HTTP Request (отправка в другой API)
```

## 💡 Советы по производительности

### Для больших объемов данных
- Используйте "Split In Batches" для обработки по частям
- Добавляйте задержки между запросами (Wait узел)
- Ограничивайте количество одновременных запросов

### Для тестирования
- Используйте фиктивные данные через "Set" узел
- Создавайте тестовые webhook локально
- Логируйте промежуточные результаты

## 🐛 Отладка

### Проблема: Workflow не выполняется
✅ Проверьте:
- Все узлы подключены?
- Нет ли ошибок в красных узлах?
- Включен ли workflow (для автоматических триггеров)?

### Проблема: Неправильные данные
✅ Действия:
1. Выполните узел по отдельности (Execute Node)
2. Проверьте входные данные в JSON view
3. Добавьте Function узел для логирования
4. Используйте "Sticky Note" для комментариев

### Проблема: Timeout при HTTP запросах
✅ Решение:
- Увеличьте Timeout в настройках узла
- Проверьте доступность API
- Добавьте retry логику

## 📊 Структура данных

### Формат данных между узлами
```javascript
[
  {
    json: {
      // Ваши данные здесь
      id: 1,
      name: "Example"
    },
    binary: {
      // Бинарные данные (файлы, изображения)
    }
  }
]
```

### Доступ к данным из предыдущих узлов
```javascript
// Текущий узел
$json.fieldName

// Предыдущий узел по имени
$node["Node Name"].json.fieldName

// Все элементы
$items

// Переменные окружения
$env.VARIABLE_NAME
```

## 🎓 Прогрессия обучения

### Неделя 1: Основы
- [ ] HTTP Request узлы
- [ ] Function узлы (базовый JavaScript)
- [ ] IF условия
- [ ] Webhook триггеры

### Неделя 2: Продвинутое
- [ ] Split In Batches
- [ ] Error handling
- [ ] Cron scheduling
- [ ] Работа с JSON/XML

### Неделя 3: Интеграции
- [ ] OAuth аутентификация
- [ ] Database операции
- [ ] Email отправка
- [ ] File operations

### Неделя 4: Оптимизация
- [ ] Sub-workflows
- [ ] Variables и Settings
- [ ] Custom nodes (опционально)
- [ ] Deployment стратегии

## 🚀 От прототипа к продакшену

Когда прототип готов:

1. **Добавьте обработку ошибок** везде
2. **Настройте логирование** для важных событий
3. **Добавьте мониторинг** (health checks)
4. **Документируйте workflow** (используйте Sticky Notes)
5. **Настройте бэкапы** (`make backup` регулярно)
6. **Протестируйте edge cases**

## 📚 Ресурсы

- [n8n Community](https://community.n8n.io/) - форум сообщества
- [n8n Workflows](https://n8n.io/workflows) - готовые примеры
- [n8n Docs](https://docs.n8n.io/) - документация
- [n8n YouTube](https://www.youtube.com/@n8n-io) - видео-туториалы

---

**Удачи в прототипировании! Экспериментируйте и создавайте! 🎉**

