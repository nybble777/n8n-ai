# 📋 Быстрая справка n8n

## 🚀 Запуск и остановка

```bash
make start      # Запустить n8n
make stop       # Остановить n8n
make restart    # Перезапустить
make logs       # Посмотреть логи
make help       # Все команды
```

**URL:** http://localhost:5678

---

## 📥 Импорт примеров

### Через UI:
1. Откройте http://localhost:5678
2. Menu (☰) → Import from File
3. Выберите файл из `examples/`

### Доступные примеры:
- ✅ `example-workflow-http.json` - HTTP запросы
- ✅ `example-workflow-webhook.json` - Webhook
- 🆕 `example-workflow-schedule.json` - Автоматика
- 🆕 `example-workflow-if-condition.json` - Условия

---

## ⚡ Активация workflow

**Для Webhook и Schedule примеров:**
1. Откройте workflow
2. Переключатель "Active" → включите
3. Должен стать зеленым ✓

---

## 🧪 Тестирование webhook

```bash
# Простой тест
curl -X POST http://localhost:5678/webhook/test-webhook \
  -H "Content-Type: application/json" \
  -d '{"message": "test"}'

# Или скрипт
./test-my-webhook.sh

# Диагностика
./check-webhook.sh
```

---

## 🔄 Hot Reload

✅ **Работает без перезапуска:**
- Изменения workflow в UI
- Function/Code узлы
- Активация/деактивация

❌ **Требует restart:**
- Переменные окружения
- Custom nodes
- `make restart`

---

## 💾 Резервные копии

```bash
make backup     # Создать бэкап
make restore    # Восстановить
```

**Файлы:** `backups/workflows-*.json`

---

## 🐛 Решение проблем

### n8n не запускается
```bash
docker ps               # Проверить статус
make stop && make start # Перезапуск
make logs               # Посмотреть ошибки
```

### Webhook 404
1. ✅ Workflow активирован?
2. ✅ Правильный URL?
3. ✅ n8n запущен?

→ Читайте: `QUICK_WEBHOOK_FIX.md`

### Ошибка импорта
→ Читайте: `EXAMPLES_UPDATED.md`

---

## 📚 Документация

| Файл | Назначение |
|------|------------|
| `START_HERE.md` | Начните здесь |
| `QUICK_START.md` | Быстрый старт |
| `EXAMPLES_UPDATED.md` | Обновления примеров |
| `QUICK_WEBHOOK_FIX.md` | Решение проблем с webhook |
| `WEBHOOK_EXAMPLES.md` | Примеры запросов |
| `PROTOTYPING_GUIDE.md` | Лучшие практики |
| `HOT_RELOAD_GUIDE.md` | Hot reload |
| `FAQ.md` | Частые вопросы |

---

## ⚙️ Основные команды

```bash
# Запуск
make start

# Статус
docker ps | grep n8n

# Логи
make logs

# Бэкап
make backup

# Остановка
make stop

# Обновление
make update
```

---

## 🎯 Быстрый старт (копируй-вставь)

```bash
# 1. Запустить n8n
cd /Users/nybble/projects/n8n
make start

# 2. Открыть в браузере
open http://localhost:5678

# 3. Импортировать пример
# (через UI: Menu → Import from File)

# 4. Тестировать
# (для Manual: Execute Workflow)
# (для Webhook: активировать + curl)
```

---

## 💡 Полезные советы

1. **Function узлы** = hot reload ✅
2. **Custom nodes** = нужен restart ❌
3. **Test URL** = один раз после Execute
4. **Production URL** = требует Active workflow
5. **Бэкапьте** перед большими изменениями

---

## 🔗 Быстрые ссылки

- **n8n UI:** http://localhost:5678
- **Документация:** https://docs.n8n.io/
- **Примеры:** https://n8n.io/workflows
- **Форум:** https://community.n8n.io/

---

**Создано для быстрого прототипирования! 🚀**

