# n8n Прототипирование 🚀

Проект для быстрого прототипирования автоматизаций с помощью n8n.

## 🎯 Что это?

Это готовая среда для разработки и тестирования workflow в n8n. Полностью бесплатная, работает локально на вашем компьютере.

## ⚡ Быстрый старт

### Требования
- Docker и Docker Compose установлены
- Порт 5678 свободен

### Запуск

1. **Клонируйте или создайте проект:**
```bash
# Если еще не сделано
cd /Users/nybble/projects/n8n
```

2. **Запустите n8n:**
```bash
docker-compose up -d
```

3. **Откройте браузер:**
```
http://localhost:5678
```

Готово! 🎉 n8n запущен и готов к работе.

## 📋 Основные команды

### Управление контейнером

```bash
# Запустить n8n
docker-compose up -d

# Остановить n8n
docker-compose down

# Посмотреть логи
docker-compose logs -f n8n

# Перезапустить
docker-compose restart

# Обновить до последней версии
docker-compose pull
docker-compose up -d
```

### Резервное копирование

```bash
# Создать резервную копию
docker exec n8n-ai n8n export:workflow --all --output=/backups/workflows-backup.json

# Импортировать workflow
docker exec n8n-ai n8n import:workflow --input=/backups/workflows-backup.json
```

## 📁 Структура проекта

```
.
├── docker-compose.yml      # Конфигурация Docker
├── .env.example           # Пример переменных окружения
├── README.md              # Эта инструкция
├── n8n-data/              # Данные n8n (создается автоматически)
│   ├── database.sqlite    # База данных
│   └── ...
├── backups/               # Резервные копии workflow
└── custom-nodes/          # Кастомные узлы (опционально)
```

## 🔧 Настройка

### Базовая аутентификация (опционально)

Если хотите защитить n8n паролем:

1. Создайте файл `.env`:
```bash
cp .env.example .env
```

2. Раскомментируйте и настройте:
```env
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=ваш_пароль
```

3. Перезапустите:
```bash
docker-compose down
docker-compose up -d
```

### Использование PostgreSQL вместо SQLite

Для больших проектов можно использовать PostgreSQL. Добавьте в `docker-compose.yml`:

```yaml
services:
  postgres:
    image: postgres:15
    container_name: n8n-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=n8n
      - POSTGRES_USER=n8n
      - POSTGRES_PASSWORD=n8n
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - n8n-network

volumes:
  postgres-data:
```

И обновите настройки n8n в `.env`.

## 🎨 Примеры использования

### 1. Простой HTTP запрос
- Добавьте узел "HTTP Request"
- Настройте GET запрос к API
- Обработайте результат

### 2. Webhook
- Используйте узел "Webhook"
- URL будет: `http://localhost:5678/webhook/ваш-путь`
- Тестируйте с помощью curl или Postman

### 3. Запланированное выполнение
- Используйте узел "Cron"
- Настройте расписание
- Добавьте действия

## 🐛 Решение проблем

### Порт 5678 занят
Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:5678"  # Теперь доступно на http://localhost:8080
```

### Проблемы с правами доступа
```bash
sudo chown -R 1000:1000 n8n-data/
```

### Очистка и начало с нуля
```bash
docker-compose down -v
rm -rf n8n-data/
docker-compose up -d
```

## 📚 Полезные ссылки

- [Официальная документация n8n](https://docs.n8n.io/)
- [Примеры workflow](https://n8n.io/workflows)
- [Сообщество n8n](https://community.n8n.io/)
- [GitHub репозиторий n8n](https://github.com/n8n-io/n8n)

## 💡 Советы для прототипирования

1. **Начните с простого** - создайте минимальный workflow и постепенно добавляйте функции
2. **Используйте webhook-узлы** для тестирования интеграций
3. **Сохраняйте версии** - экспортируйте workflow перед большими изменениями
4. **Логируйте данные** - используйте узел "Function" для отладки
5. **Тестируйте по частям** - запускайте отдельные части workflow

## 🎓 Следующие шаги

После того как освоитесь с основами:
- Изучите кастомные узлы
- Попробуйте интеграции с популярными сервисами
- Настройте обработку ошибок
- Создайте сложные workflow с ветвлением

---

**Удачи в прототипировании!** 🚀

Если возникнут вопросы, обращайтесь к документации или сообществу n8n.

