# ✅ Правильные значения DB_TYPE для n8n

## ❌ Частые ошибки

```
DB_TYPE=PostgreSQL    ❌ Неправильно!
DB_TYPE=postgres      ❌ Неправильно!
DB_TYPE=postgresql    ❌ Неправильно!
DB_TYPE=pg            ❌ Неправильно!
```

---

## ✅ Правильные значения

### SQLite (для тестов)

```
DB_TYPE=sqlite
```

**Плюсы:**
- ✅ Простая настройка (одна переменная)
- ✅ Работает сразу
- ✅ Не нужна отдельная база

**Минусы:**
- ⚠️ Данные временные на бесплатном Render.com
- ⚠️ Могут потеряться при рестарте

**Использовать для:**
- Тестирования
- Прототипирования
- Быстрого старта

---

### PostgreSQL (для production)

```
DB_TYPE=postgresdb
DATABASE_URL=postgres://user:pass@host:5432/db
```

**Обратите внимание:** именно `postgresdb`, не `PostgreSQL`!

**Плюсы:**
- ✅ Данные постоянные
- ✅ Переживают рестарты
- ✅ Подходит для production

**Минусы:**
- ⚠️ Нужно создать PostgreSQL базу
- ⚠️ Две переменные вместо одной

**Использовать для:**
- Production
- Долгосрочное использование
- Важные данные

---

### MySQL (если используете MySQL)

```
DB_TYPE=mysqldb
DATABASE_URL=mysql://user:pass@host:3306/db
```

---

### MariaDB (если используете MariaDB)

```
DB_TYPE=mariadb
DATABASE_URL=mysql://user:pass@host:3306/db
```

---

## 🔍 Как n8n определяет тип базы

n8n проверяет переменную `DB_TYPE` и ожидает **точное** совпадение:

```javascript
// Внутри n8n
switch(process.env.DB_TYPE) {
  case 'sqlite':
    // Используем SQLite
    break;
  case 'postgresdb':
    // Используем PostgreSQL
    break;
  case 'mysqldb':
    // Используем MySQL
    break;
  case 'mariadb':
    // Используем MariaDB
    break;
  default:
    // Пытаемся использовать PostgreSQL по умолчанию
    // Ищем DATABASE_URL
    break;
}
```

Если значение не совпадает точно, n8n использует дефолт (PostgreSQL) и ищет `DATABASE_URL`.

---

## 🔧 Исправление ошибки

### Если у вас `DB_TYPE=PostgreSQL`:

1. **Откройте Render.com Dashboard**
2. **Ваш сервис → Environment**
3. **Найдите переменную `DB_TYPE`**
4. **Кликните на значение**
5. **Измените на:**
   - `sqlite` (если не нужна постоянная БД)
   - `postgresdb` (если нужна PostgreSQL)
6. **Save Changes**
7. **Подождите ~2 минуты**

### Для SQLite:

Просто измените:
```
DB_TYPE=sqlite
```

Всё! Больше ничего не нужно.

### Для PostgreSQL:

1. **Измените:**
   ```
   DB_TYPE=postgresdb
   ```

2. **Создайте PostgreSQL базу:**
   - Dashboard → New → PostgreSQL
   - Name: `n8n-db`
   - Region: Frankfurt
   - Plan: Free
   - Create Database

3. **Скопируйте Internal Database URL**

4. **Добавьте переменную:**
   ```
   DATABASE_URL=[Internal Database URL]
   ```

5. **Save Changes**

---

## 📊 Сравнение

| Вариант | DB_TYPE | Доп. переменные | Постоянность данных |
|---------|---------|-----------------|---------------------|
| SQLite | `sqlite` | Нет | ⚠️ Временные |
| PostgreSQL | `postgresdb` | `DATABASE_URL` | ✅ Постоянные |
| MySQL | `mysqldb` | `DATABASE_URL` | ✅ Постоянные |
| MariaDB | `mariadb` | `DATABASE_URL` | ✅ Постоянные |

---

## ⚠️ Важно!

### Регистр имеет значение

```
DB_TYPE=sqlite      ✅ Правильно
DB_TYPE=SQLite      ❌ Неправильно
DB_TYPE=SQLITE      ❌ Неправильно
```

### Точное написание

```
DB_TYPE=postgresdb  ✅ Правильно
DB_TYPE=postgres    ❌ Неправильно
DB_TYPE=PostgreSQL  ❌ Неправильно
```

---

## 💡 Рекомендация

### Для быстрого старта:

```
DB_TYPE=sqlite
```

Просто и работает сразу!

### Для серьезного использования:

```
DB_TYPE=postgresdb
DATABASE_URL=postgres://...
```

Данные сохраняются навсегда.

---

## 🆘 Если все еще ошибка

### Ошибка: `connect ECONNREFUSED ::1:5432`

**Причина:** n8n пытается подключиться к PostgreSQL

**Решение:**
1. Убедитесь что `DB_TYPE=sqlite` (маленькими буквами!)
2. Или добавьте `DATABASE_URL` если хотите PostgreSQL

### Ошибка: `Invalid database type`

**Причина:** Неправильное значение DB_TYPE

**Решение:**
Используйте только: `sqlite`, `postgresdb`, `mysqldb`, `mariadb`

---

## ✅ Итого

**Правильные значения:**
- `sqlite` - для SQLite
- `postgresdb` - для PostgreSQL (не `PostgreSQL`!)
- `mysqldb` - для MySQL
- `mariadb` - для MariaDB

**Регистр важен!** Используйте маленькие буквы.

**Для быстрого старта:** `DB_TYPE=sqlite`

**Для production:** `DB_TYPE=postgresdb` + `DATABASE_URL`

