# 🔄 Переименование проекта: n8n-prototype → n8n-ai

## ✅ Выполнено

Заменено **n8n-prototype** на **n8n-ai** в 19 файлах:

### Основные конфигурации:
- ✅ `README.md`
- ✅ `docker-compose.yml`
- ✅ `docker-compose.postgres.yml`
- ✅ `render.yaml` (+ обновлен на SQLite)
- ✅ `Makefile`
- ✅ `fly.toml`

### Документация:
- ✅ `FAQ.md`
- ✅ `DEPLOYMENT_GUIDE.md`
- ✅ `PUBLIC_ACCESS_GUIDE.md`
- ✅ `SECURITY_SETUP.md`
- ✅ `FLYIO_DEPLOY_STEPS.md`
- ✅ `HOT_RELOAD_GUIDE.md`
- ✅ `IMPORT_GUIDE.md`
- ✅ `WEBHOOK_EXAMPLES.md`
- ✅ `EXAMPLES_UPDATED.md`
- ✅ `examples/README.md`

### Скрипты:
- ✅ `start-public.sh`
- ✅ `check-webhook.sh`
- ✅ `scripts/export-workflows.sh`
- ✅ `scripts/import-workflows.sh`

---

## 🎯 Важные изменения в render.yaml

Обновлена конфигурация для использования **SQLite** (для прототипирования):

```yaml
services:
  - type: web
    name: n8n-ai  # ← Изменено
    envVars:
      - key: DB_TYPE
        value: sqlite  # ← Изменено на SQLite
      # DATABASE_URL удален
```

Секция `databases` закомментирована (не нужна для SQLite).

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Настройте Render UI

**Локальные файлы обновлены, но это НЕ влияет на уже созданный сервис!**

### Обязательные шаги для запуска n8n:

1. **Откройте Render Dashboard:**
   ```
   https://dashboard.render.com
   ```

2. **Перейдите к сервису:**
   - Найдите `n8n-ai` в списке
   - Кликните на него

3. **Измените Environment Variables:**
   - Левое меню → **Environment**
   
   **Измените:**
   ```
   DB_TYPE = sqlite
   ```
   
   **Удалите полностью:**
   ```
   DATABASE_URL  (🗑️ иконка справа)
   ```
   
   **Также удалите (если есть):**
   ```
   DB_POSTGRESDB_*
   POSTGRES_*
   ```

4. **Сохраните:**
   - Прокрутите вниз
   - Кликните **Save Changes** (синяя кнопка)
   - Дождитесь: "Environment variables updated" ✅

5. **Принудительный деплой:**
   - Левое меню → **Manual Deploy**
   - Кликните **Deploy latest commit**

6. **Проверьте логи:**
   - Левое меню → **Logs**
   - Ждите (~2-3 минуты):
   
   ```
   ✅ n8n ready on port 5678
   ✅ Editor is now accessible via:
      https://n8n-ai.onrender.com
   ```

---

## 🔍 Проверка

После деплоя проверьте:

```bash
# Откройте в браузере:
https://n8n-ai.onrender.com

# Или через curl:
curl -I https://n8n-ai.onrender.com
```

Должно вернуть `HTTP/2 200` ✅

---

## 📊 Текущее состояние

### Локально (файлы):
- ✅ Все файлы обновлены на `n8n-ai`
- ✅ `render.yaml` настроен на SQLite
- ✅ Готово к коммиту

### На Render.com:
- ⏳ **Требуется ручная настройка Environment Variables**
- ⏳ **Требуется Manual Deploy**

---

## 💡 Почему нужна ручная настройка?

`render.yaml` используется **только при первом создании сервиса**.

Вы создали сервис через UI → настройки хранятся в Render, а не в файле.

**Решение:** измените Environment Variables через Dashboard.

---

## 🚀 Следующие шаги

1. **Сейчас:** измените Environment Variables в Render UI (шаги выше)
2. **После запуска:** откройте `https://n8n-ai.onrender.com`
3. **Войдите:** используйте Basic Auth (admin / пароль из Environment)
4. **Протестируйте:** импортируйте workflow и проверьте webhook

---

## 📝 Если нужен PostgreSQL позже

Раскомментируйте в `render.yaml`:

```yaml
databases:
  - name: n8n-ai-db
    plan: free
    databaseName: n8n
    user: n8n
    region: frankfurt
```

И измените:
```yaml
envVars:
  - key: DB_TYPE
    value: postgresdb
  
  - key: DATABASE_URL
    fromDatabase:
      name: n8n-ai-db
      property: connectionString
```

Но для прототипирования **SQLite достаточно**! 🎉

---

## 🎉 Итог

- ✅ Все файлы переименованы
- ✅ Конфигурация обновлена на SQLite
- ⏳ Осталось: настроить Environment Variables в Render UI

**Время до запуска: 5 минут** (после настройки Environment)

